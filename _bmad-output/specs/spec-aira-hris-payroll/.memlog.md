---
topic: Aira — multi-tenant HRIS + simple payroll SaaS for Indonesian SMBs (20–150 employees), self-serve, Rp15.000/employee/month
updated: 2026-08-21T16:17
---

- (event) Create run: distilled docs/01-07 (1373 lines, 7 files) into SPEC kernel + companions; input rich, express-extract, no elicitation needed
- (direction) User chose whole-product scope (Phase 0-4) over Phase 0+1 or Phase 0 only; rationale: Phase 0 is non-retrofittable and its shape is driven by payroll requirements, so payroll must be in the contract
- (decision) Slug aira-hris-payroll; docs/ is a multi-source folder with no inherited slug
- (decision) Docs technical decisions treated as ratified, not reopened; this run distills, it does not re-litigate
- (decision) Seven spec-authored companions: statutory-rules.md, data-model.md, stack.md, conventions.md, test-contract.md, commercial-model.md, roadmap.md
- (decision) docs/01-07 listed as sources (fully absorbed), not companions; CLAUDE.md stays repo-level agent instruction, not a spec companion
- (decision) Document language English per config document_output_language and CLAUDE.md rationale (docs sit next to code); user-facing UI copy Indonesian is a constraint
- (capability) CAP-1 Tenant isolation: pooled shared schema, tenant_id + RLS on every table; success = isolation suite green in CI, catalog sweep finds no table without RLS+policy
- (capability) CAP-2 Identity and membership: phone+OTP or employee-ID+PIN auth, tenant_id in app_metadata, many-to-many memberships, one active company per JWT
- (capability) CAP-3 Statutory rate registry: TER/PTKP/BPJS/overtime rates as dated global seed data, tenant-uneditable
- (capability) CAP-4 Job runtime: dedicated worker + queue, every job idempotent, resumable, concurrency-limited, no service_role
- (capability) CAP-5 Attendance photo storage: client-compressed upload direct to R2 via signed URL, thumbnail at upload, dated retention
- (capability) CAP-6 Employee records with dated assignments: position/department/branch/manager/employment-type as valid_from rows; position-on-date query is historically correct
- (capability) CAP-7 Excel import wizard: 200 employees, validation report, preview before commit, idempotent re-run after fixes
- (capability) CAP-8 Salary components: parameterised calc types plus seven boolean treatment flags, dated per employee
- (capability) CAP-9 PII protection: NIK and salary role-restricted and access-logged; audit log on every payroll-affecting mutation
- (capability) CAP-10 Clock in/out with GPS geofence against branch radius, photo, out-of-range reason+flag, mock-location flagging
- (capability) CAP-11 Offline attendance queue with client_uuid idempotency; repeated submit produces one row
- (capability) CAP-12 Night-shift attribution: work_date = shift start date, one shift = one attendance row, work minutes correct across midnight
- (capability) CAP-13 Attendance locking: lock is a separate step from payroll lock; post-lock corrections need an authorised role and are audit-logged
- (capability) CAP-14 Attendance recap and aggregate reporting from materialized views, refreshed nightly with incremental current-day
- (capability) CAP-15 Leave: requests, dated balances with carry-over, overlap rejection, coverage conflict warning, balance decremented on final approval
- (capability) CAP-16 Shift scheduling: templates with crosses_midnight and late tolerance, bulk assignment by dept/branch over a date range, coverage view
- (capability) CAP-17 Payroll calendars and periods: calendar-month and cut-off models both produce correct tax month from payment date; periods generated 24 months ahead
- (capability) CAP-18 Gross-to-net pipeline: explicit ordered steps, each writing payroll_items with inputs in meta; every payslip figure traceable to its inputs
- (capability) CAP-19 Overtime: hourly wage = monthly/173, base from base_for_overtime flags, multipliers from a dated table; per-company auto-from-attendance vs approved-request, default request
- (capability) CAP-20 PPh21: TER lookup Jan-Nov, progressive annual reconciliation in December or final month, negative (refund) result supported
- (capability) CAP-21 BPJS: five programs, employee and employer portions, per-company JKK risk class, dated wage caps, base from base_for_bpjs_* flags
- (capability) CAP-22 THR: separate run_type, entitlement after 1 month, proportional under 12 months, base from base_for_thr flags
- (capability) CAP-23 Run immutability: locking snapshots config as used and freezes payslips; corrections are a new run in the same period, never an edit
- (capability) CAP-24 YTD: employee_ytd maintained by calculation and importable for mid-year onboarding, so December reconciliation is correct
- (capability) CAP-25 Payslip: immutable snapshot plus PDF, reprint reproduces original figures
- (capability) CAP-26 Exports: bank bulk-transfer file per bank spec, 1721-A1 data, BPJS reporting, journal CSV
- (capability) CAP-27 Generic approval engine: routes target position_id and department path, configurable levels per request type, delegation, timeout escalation; new approvable entity needs no engine change
- (capability) CAP-28 Configuration: payroll-affecting config dated not mutable, industry presets, holiday calendar seeded and per-company editable, dry-run sample payslips before commit
- (capability) CAP-29 Billing and subscription: org-level headcount snapshot, proration, dunning, overdue degrades to read-only that still allows payslip download and export
- (capability) CAP-30 Self-serve onboarding: signup to completed payroll with zero human contact, under 30 minutes of configuration
- (capability) CAP-31 Support deflection: knowledge base, short videos, WhatsApp bot for repeat questions, in place of staffed support
- (constraint) tenant_id = company_id = one legal entity (PT), not a group and not a branch; NPWP, NPP BPJS, PKWT and 1721-A1 all attach to a legal entity
- (constraint) tenant_id on every table including lookup and log tables; only global stat_* reference tables are exempt and must be explicitly allowlisted
- (constraint) RLS enabled AND forced on every table; policies wrap claims as (select auth.tenant_id()) or Postgres re-evaluates per row (10-100x on multi-million-row attendance)
- (constraint) Every index leads with tenant_id, otherwise it is unusable once RLS adds its predicate
- (constraint) tenant_id lives in app_metadata, never user_metadata (user_metadata is user-writable)
- (constraint) Workers never use service_role; dedicated role with FORCE ROW LEVEL SECURITY and per-transaction tenant context
- (constraint) No cross-tenant bypass role; group access is explicit memberships rows resolved through a view, one active company per token
- (constraint) Money is integer rupiah (bigint), rounded per component at the moment it is written, so gross/total_deduction/net are exact sums of stored lines; no float, no decimal-as-string, no currency library
- (constraint) Anything affecting payroll is versioned with valid_from/valid_to, never overwritten: rates, BPJS, salary components, org assignments, payroll config
- (constraint) A locked payroll run is immutable through every code path including the worker role
- (constraint) Statutory rates are ours: seeded, dated, identical across tenants, not editable by tenants
- (constraint) No formula builder; salary components are parameterised types plus seven boolean treatment flags
- (constraint) Attendance photos upload client to R2 directly via signed URL, never through a Next.js route handler; no public buckets, short-TTL signed URLs only
- (constraint) One Supabase project for all tenants; never one project or one schema per client
- (constraint) work_date = the date the shift STARTS; changing this after production data exists means recalculating every attendance row
- (constraint) Approvals target position_id and department path, never user_id
- (constraint) Payroll calculation is a pure function of its snapshot: same inputs always produce the same output
- (constraint) Infra cost stays under 6% of revenue (~USD 180/month at 100 clients / 4.000 employees); a design choice that breaks this must be flagged
- (constraint) Self-serve is mandatory: no manual onboarding, no training sessions, no price negotiation
- (constraint) A new configuration option is added only when three separate clients have asked for the same thing
- (constraint) Phase 0 foundation completes before anything else; it is the only category of mistake that requires rewriting rather than extending
- (constraint) Every rate marked VERIFY must be source-checked against DJP/BPJS/Kemnaker publications before seeding
- (constraint) No Supabase Realtime; poll at 30s intervals (concurrent connections are billed)
- (constraint) All aggregate reporting goes through materialized views; live aggregate queries force the compute upgrade path
- (constraint) Every migration that creates a table must in the same file enable RLS, force RLS, create the tenant policy, and create the tenant_id-leading index; never loop over tenants in a migration
- (constraint) User-facing UI copy is Indonesian; documentation is English; Indonesian regulatory terms stay in Indonesian
- (constraint) Overdue accounts degrade to read-only, never blocked; withholding payroll data is the fastest way to lose reputation
- (decision) CAP-9 widened to carry the UU PDP obligations from docs/07 (DPA with clients, retention policy, data deletion flow) rather than spawning a separate compliance capability
- (decision) Phase 4 depth items (multi-PT consolidated reporting, face matching, fingerprint devices, weekly payroll) are recorded in roadmap.md as rule-of-three gated, not as committed capabilities
- (assumption) docs/01-07 technical decisions are ratified as-is; this spec distills them and does not reopen settled calls (rounding, work_date, pooled tenancy, no formula builder)
- (assumption) PWA-first client per CLAUDE.md; docs/02 is silent on client platform
- (assumption) Tier feature split (Free 10 employees / Core Rp10.000 / Payroll Rp15.000) is as stated in docs/01 and is load-bearing for CAP-29
- (assumption) Attendance photo retention is 6 months on paid tiers and 30 days on free, per docs/01 and docs/02
- (question) Statutory VERIFY register: every rate and rule marked VERIFY in statutory-rules.md needs a source check against DJP/BPJS/Kemnaker before seeding — TER category mapping, PTKP amounts, non-NPWP surcharge, biaya jabatan cap, BPJS rates and caps, holiday overtime multipliers for 5- vs 6-day weeks, THR treatment under TER, employer BPJS as taxable benefit, PPh21 and BPJS rounding units
- (question) Distribution: where do the first 100 clients come from? docs/06 names this the largest risk in the project and leaves it unresolved; strongest candidate is the existing outsourcing client base. Must resolve before Phase 2
- (question) Mock-location detection (M2 acceptance in docs/05) is an Android native capability and is not available to a PWA. Does CAP-10 drop mock-location flagging until a native shell exists, ship a weaker web-side heuristic, or does PWA-first change?
- (question) attendances unique key is (tenant_id, employee_id, work_date, client_uuid), which permits several rows per employee per work_date with different client_uuids — this conflicts with the docs/04 rule that one shift is always one attendances row. Which invariant wins?
- (question) No payment gateway is chosen anywhere in docs; CAP-29 needs one, and docs/01 makes gateway fees part of the annual-billing rationale
- (question) WhatsApp provider and cost model for OTP (CAP-2) and notifications is unmodelled; docs/05 warns per-message cost can rival infrastructure cost
- (question) Bank bulk-transfer file specs for BCA, Mandiri, BNI, BRI need current-spec verification before CAP-26 is implementable
- (question) payroll_calendars.period_type includes weekly, but weekly payroll is listed as Phase 4 build-on-demand — is weekly in scope for CAP-17 or schema-only for now?
- (event) Self-validate pass 1 (coherence) PASS: 31 capabilities each with intent+success; CAP ids 1-31 contiguous and unique; 27 constraints all rule something out; 12 non-goals; success signal is the M3 parallel-run rupiah reconciliation plus the M4 zero-human-contact signup, both demonstrable; all 7 companion refs and all 7 source paths resolve on disk
- (event) Self-validate pass 2 (preservation) PASS: swept docs/01-07 claim by claim across 77 distinctive markers (pricing floor, annual 12-for-14, USD180 infra ceiling, rule of three, Supavisor, statement_timeout, DETACH PARTITION, Vercel Image Optimization, log drains, Singapore residency, ltree, original_join_date, config_snapshot, client_uuid, 173 divisor, biaya jabatan, KEP-102, PMK 168, WFH trap, all five employment types, golden files, byte-identical reprint, Zod, lib/domain, UU PDP/DPA, presets, dry-run, dunning, read-only overdue, all performance budgets) - every one landed in SPEC.md or a companion, zero gaps
- (event) Wrapper-only content dropped: doc numbering headings, inter-doc cross-references (docs/NN pointers rewritten to companion names), and the docs/04 preamble about why rates are indicative (its substance is preserved as the VERIFY register)
- (event) Spec finalized: SPEC.md + 7 spec-authored companions written to _bmad-output/specs/spec-aira-hris-payroll/
- (direction) User supplied a Claude Design project (dcaaa7ad-e795-4fad-8b3e-223f30a4ad1d, 'HRIS project UI mockup') containing HRIS Dashboard.dc.html on the Nocturne design system, and asked to document the design first rather than implement it in code
- (event) Read via DesignSync: HRIS Dashboard.dc.html, _ds/nocturne-*/styles.css, _ds/nocturne-*/readme.md, support.js. Skipped _ds_bundle.js: readme states styles.css is the only stylesheet and the source of truth for the look; support.js is the generated dc-runtime canvas renderer, not app code
- (decision) Two new spec-authored companions: design-system.md (Nocturne tokens, the app-level --ui-* layer, theming) and screen-dashboard.md (the dashboard screen spec). Registered in SPEC.md companions
- (decision) Canvas primitives are translated, not ported: sc-for to map, sc-if to conditional render, style-hover to CSS classes, DCLogic state to component state, {{ }} to expressions. support.js and _ds_bundle.js have no place in the Next.js build
- (decision) Supabase public schema confirmed empty (zero tables), so the dashboard is documented against typed fixtures; no data layer is specified beyond the shape each region binds to
- (constraint) The --ui-* variables are an app-level semantic layer defined per theme, NOT part of Nocturne styles.css; they must be declared in app CSS or the screen renders unstyled
- (note) Mockup arithmetic validates the per-component rounding rule: earnings 358.400.000 + 47.320.000 + 21.640.000 = 427.360.000 gross exactly, deductions 12.960.000 + 9.402.000 + 5.150.000 = 27.512.000 exactly
- (question) Mockup sidebar states attendance photos older than 90 days are purged, but the retention policy is 6 months on paid tiers and 30 days on free. 90 days matches neither — which is correct?
- (question) Mockup UI copy is English while CLAUDE.md makes Indonesian user-facing copy a definition-of-done item. Documented copy is specified in Indonesian; confirm before build
- (question) Small accent-colored text (10-11px labels using --ui-accent-text) sits below the DS accent-on-ground 3:1 contrast threshold, which the Nocturne readme reserves for chrome and large text. Use --color-accent-300 on the dark ground for those labels?
- (event) SPEC.md re-derived: companions now 9 (added design-system.md, screen-dashboard.md); open questions now 10 (added the photo-retention three-way conflict and the missing empty/loading/error + responsive + switcher-menu design states)
- (event) Design documented, not implemented: no code written, Next.js scaffold deliberately not created at user direction
- (direction) User asked to document the dashboard's empty, loading and error states — a new design pass, authored here rather than read from the canvas (the project holds only the one happy-path artboard)
- (decision) New spec-authored companion screen-dashboard-states.md, sibling to screen-dashboard.md, so the happy-path spec stays readable
- (decision) Governing principle for all non-happy states: never render a number you cannot stand behind. Unknown renders as an em dash and never as zero, stale renders with a freshness stamp, a failed region says it failed rather than showing empty. Derived from the product's own unsurvivable failure mode (a wrong payslip)
- (decision) Empty is decomposed into three distinct causes rather than one state: day-0 new tenant (onboarding checklist replacing the grid), partially onboarded (real headcount, unknown attendance), and steady-state-clear (no approvals or warnings pending, rendered as positive confirmation not a void)
- (decision) Read-only mode (CAP-29 overdue billing) documented alongside the three requested states: it is not an error but it changes exactly the same affordances, and the rule is degrade-never-block with payslip download and export preserved
- (decision) Stale-data state added: aggregate regions read from nightly materialized views, so a failed refresh silently serves yesterday's attendance figures - unacceptable in a payroll surface without a freshness stamp
- (decision) Page-level tenant-resolution failure fails closed: no sidebar data, no company name, no partial render. Security posture from the cross-tenant leak risk, not a UX preference
- (decision) Payroll calculating and payroll job-failed are separate designed states, not loading spinners: jobs are resumable and record progress, and conventions.md requires failures be surfaced never silently swallowed
- (note) Skeletons use --ui-track fill with a quiet opacity pulse and never animate the accent, per the Nocturne no-flood rule; 200ms delay before showing and 400ms minimum once shown, to prevent flash and flicker
- (event) screen-dashboard-states.md written and registered; SPEC.md companions now 10; the missing-states open question narrowed to what genuinely remains (responsive breakpoints, company-switcher menu, notification bell); screen-dashboard.md cross-linked
- (direction) User said continue; the canvas holds only the one artboard so there is no next screen to document. Closing the three design gaps SPEC.md still names as unspecified: responsive breakpoints, the company-switcher menu, and notification-bell behaviour
- (decision) New spec-authored companion screen-dashboard-interaction.md covering responsive behaviour, the two undrawn affordances, and accessibility
- (decision) Responsive floor rather than a mobile redesign: the HR dashboard is a desk surface and the PWA-first assumption is driven by employee clock-in, not HR admin. Below 768px the screen must be readable and operable, not re-conceived
- (decision) Single-column stacking order is by decision urgency, not source order: payroll run, needs action, compliance warnings, attendance table, audit trail
- (decision) Company switching reissues the token per CAP-2, so it is a full session-context change: panel closes, page returns to the dashboard root, full loading state. A deep link belonging to the previous company must not survive the switch
- (decision) Single-company tenants render the switcher as a plain label with no caret and no menu; a dropdown with one item is noise
- (decision) The in-app bell is the free notification channel and absorbs everything; WhatsApp carries only digests and true escalations. Each notification type declares its channel, because docs/05 warns per-message cost can rival infrastructure cost
- (note) Accessibility defects found in the artboard: breakdown rows are div+onClick so they are not keyboard operable and must become button with aria-expanded; the range segmented control uses bare spans instead of the DS .seg-opt native-radio pattern, losing keyboard and checked semantics; icon-only buttons have no accessible name; nav active item lacks aria-current
- (question) Does the HR dashboard need a true mobile experience, or is the sub-768px floor (readable and operable) sufficient? PWA-first was assumed for employee clock-in, not HR admin
- (event) screen-dashboard-interaction.md written and registered; SPEC.md companions now 11; the dashboard design-gap open question rewritten to point at the three documents' own open sections and to state plainly that no other screen in the product has been designed
- (direction) Update run: adopt the finalized architecture spine as an ADOPTED companion. bmad-architecture owns ARCHITECTURE-SPINE.md; this spec references it and never edits it
- (decision) ARCHITECTURE-SPINE.md registered as adopted companion at ../../planning-artifacts/architecture/architecture-Aira-2026-08-20/. 27 ADs now govern how the capabilities are built; downstream cites AD ids
- (decision) CAP-2 amended: intent and success no longer claim employee-ID+PIN reaches a session. Spine AD-7 makes the signup and administrator path Supabase email auth, and defers the employee credential to Phase 1. CAP-2 id preserved; the multi-company switching criterion is unchanged and now names the access-token hook
- (assumption) docs/03's ban on email/password is scoped to the employee population - its stated rationale is warehouse staff, SPG and field workers. The signup and administrator path uses email auth per AD-7. The employee-facing rule stands
- (note) Open question 'payment gateway' is unchanged as a question but the spine deferred it explicitly to Phase 3 with a reason, so it is no longer an unowned gap
- (note) Open question 'WhatsApp provider' narrows: AD-7 removes OTP from Phase 0 entirely, so the remaining question is only the notification-digest channel. WhatsApp OTP would force Twilio if it ever returns, since Supabase supports the WhatsApp channel only via Twilio and Twilio Verify
- (note) The deploy-skew question raised during the architecture run is resolved by AD-26 and was never a SPEC-level open question; recorded here so the two memlogs do not disagree
- (event) Self-validate pass 1 (coherence) PASS: 31 capabilities each with intent+success, ids 1-31 contiguous and unique and unchanged by this update; 27 constraints, 12 non-goals, success signal untouched; all 12 companion paths resolve on disk including the adopted spine two directories up; both AD ids cited in SPEC.md exist in the spine
- (event) Self-validate pass 2 (preservation) PASS: the only claim removed was CAP-2's assertion that employee-ID+PIN reaches a session, which is superseded by AD-7 and carried forward in the spine's Deferred section and a new SPEC assumption - nothing dropped silently. Every other capability, constraint, non-goal and open question survived the edit unchanged
- (event) Spine adopted; SPEC.md re-derived
- (decision) memberships.role enum gains accountant, synced from architecture AD-33. The role set is fixed and not tenant-customisable; owner is not a membership role but organizations.owner_user_id above the tenant boundary. data-model.md updated so spec and spine do not silently diverge
- (note) AD-31 in the spine was corrected during the role discussion: hr_staff does not see salary, which docs/05 M1 already implied by restricting NIK and salary by role. The spec's CAP-9 success criterion is unchanged and now has a concrete tier table behind it
- (decision) Design layer switched to Tailwind 4.3.3 + shadcn 4.18.0 with Nocturne demoted to token source (architecture AD-22 amended, AD-28 amended, AD-36 added). design-system.md gains a class-to-component mapping table and marks its component-class section superseded; the three screen docs gain a pointer note. Their sizes, spacing, colours and behaviour are unchanged and still binding
- (decision) Synced from architecture AD-37 and AD-38, found by the readiness gate: memberships gains last_active_at as the active-company store, and a new pii_access_logs table carries read-access logging separately from mutation-shaped audit_logs, one row per request rather than per record
- (decision) data-model.md auth paragraph corrected: it still read 'Auth is phone + OTP, or employee ID + PIN. Not email/password' unqualified, contradicting adopted AD-7 and AD-29. Rewritten to keep the employee-population constraint (no work email required) while naming the actual mechanism: email for signup and admin, token-based invitation for employees, account optional. Found by the Epic 1 context compilation
- (decision) employee_assignments.payroll_calendar_id starts nullable and Epic 5 tightens it. Found by the Epic 1 context compilation: the column was not null while payroll_calendars is Epic 5, so Story 1.8 and the Story 1.9 import of 200 employees could not have written a single row. Rejected creating payroll_calendars early, which would drag period_type, cutoff days, proration_basis and holiday_rule into an epic with no payroll and force invented defaults. Epic 5 carries the backfill as an inherited obligation
