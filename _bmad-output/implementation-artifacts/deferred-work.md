# Deferred Work

Findings surfaced by review that are real but not caused by, or not in scope for, the story that surfaced them.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffold-the-repository.md`
  summary: Nothing anywhere enforces the `service_role` prohibition, and no lint rule keeps `lib/db` out of `app/` or `app/` out of `worker/`.
  evidence: Verified by grep — zero occurrences of `service_role` in any lint rule, CI step or test, and zero cross-layer file globs beyond `lib/domain`. CLAUDE.md rule 5 and AD-16 make this the invariant whose violation is a cross-tenant leak. Story 1.1 built 60 denials and 70 tests for core purity while the higher-stakes invariant has none. A reviewer put it as: the invariant that got machinery is not the one that ends the business.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffold-the-repository.md`
  summary: `npm run test:isolation` exits 0 over an empty directory, with no vanished-suite guard.
  evidence: `REQUIRED_SUITES` in `vitest.config.mts` protects only `tests/boundary.test.ts`. docs/07 calls tenant isolation "the most important test in the codebase". Story 1.4 owns the suite and must extend the guard when it lands.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffold-the-repository.md`
  summary: docs/07 specifies the isolation suite at `tests/isolation.test.ts`; the scaffold created `tests/isolation/**` and excludes it from CI.
  evidence: Doc and tree disagree from day one. Recorded only in README and a Vitest comment, not in the document the project treats as the contract.
  resolved: Story 1.4 rewrote the "Required test: tenant isolation" section of `docs/07-conventions-and-testing.md` against the harness that now exists — file map, the property it enforces, the per-relation-kind rules, the two non-vacuity rules, and the exemption list. `CLAUDE.md`'s definition of done now points at a description that matches the code.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffold-the-repository.md`
  summary: `app/page.tsx` ships the create-next-app template — English copy and Vercel marketing links under `<html lang="id">`.
  evidence: CLAUDE.md's definition of done requires Indonesian user-facing strings. The spec's Never list scoped UI work to Stories 1.2–1.3, so this is deliberate deferral rather than an oversight, but it is user-facing English shipping today.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffold-the-repository.md`
  summary: `AD-*` identifiers are cited throughout the code but resolve to nothing a code reader can reach.
  evidence: `.env.example` cites AD-4/16/20 and `eslint.boundary.mjs` cites AD-2; no `AD-` identifier appears under `docs/`. The spine lives in `_bmad-output/planning-artifacts/`, which CLAUDE.md's "Where things are" map does not list.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffold-the-repository.md`
  summary: `next.config.ts` is the untouched placeholder — no security headers, no CSP, no `poweredByHeader: false`, no `images.remotePatterns` for the R2 origin.
  evidence: A multi-tenant payroll product storing NIK, salary and attendance photos ships with framework defaults.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffold-the-repository.md`
  summary: One root tsconfig with DOM lib and `jsx: react-jsx` also governs `worker/`, and `target` is the starter's ES2017.
  evidence: A long-lived Node worker is typechecked with browser types; `window`/`document` type-resolve inside `lib/domain`, where only lint stops them. No `worker` script exists in package.json either.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffold-the-repository.md`
  summary: No formatter, no format gate, no `.editorconfig`.
  evidence: A tree that deliberately mixes generated starter files with hand-written ones is exactly where formatting drift starts, and a scaffold commit is the cheapest moment to fix it.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffold-the-repository.md`
  summary: CI runs the full job twice for a push to a branch with an open PR, and lints the tree twice per job.
  evidence: `on: push` and `on: pull_request` carry no branch filters and there is no `concurrency` cancel; `npm test` spawns a second full-tree `npm run lint` inside the boundary suite.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffold-the-repository.md`
  summary: Every CI build depends on network access to Google Fonts via `next/font/google` (Geist).
  evidence: A build-time external dependency that will fail closed on a network blip, never explicitly decided.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-design-system-foundation.md`
  summary: Vendored shadcn primitives need an app-level wrapper layer — they ship English copy and non-token colours, and hand-editing them is silently reverted by the next `shadcn add`.
  evidence: `components/ui/dialog.tsx` carries `"Close"` twice as user-facing English against a definition of done requiring Indonesian, and `DialogOverlay` hardcodes `bg-black/10`, a near-invisible scrim over `--color-bg: #161826`. The frozen spec block forbids hand-editing `components/ui/`, and the engineering reason holds independently: a fix applied there disappears without warning on regeneration. The pattern applies to every primitive needing Indonesian copy, not only dialog, which makes it structural work for Story 1.3's shell rather than a patch here.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-design-system-foundation.md`
  summary: The light theme is unreachable — nothing in the repo writes the theme preference, and `prefers-color-scheme` is no longer consulted.
  evidence: No `setItem` call exists anywhere under `app/`, `components/` or `lib/`; the resolver only reads `localStorage.aira-theme`. The starter honoured OS preference and that block was removed. Until Story 1.3 ships a toggle, every user is locked to dark regardless of system preference, and roughly forty lines of light-theme CSS plus half the theme suite guard a state no one can reach.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-design-system-foundation.md`
  summary: Nocturne's 0.70x density scale is declared but inert — `--space-*` has no consumer and `p-4` is still 16px.
  evidence: Setting Tailwind's `--spacing` to the Nocturne scale would rescale every shadcn component and every `size-*` icon at once, so the reconciliation was deliberately deferred. Until it happens the product's spacing is Tailwind's, not Nocturne's, and the tokens are dead weight that reads as if it were wired.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-design-system-foundation.md`
  summary: Any future `no-restricted-syntax` block is a live hazard to the AD-2 purity boundary.
  evidence: ESLint replaces rule options rather than merging them, so a new block whose `files` glob overlaps `lib/domain/**` silently deletes every purity denial while lint still exits zero. This happened during this story and only the boundary suite caught it, with 68 failures. Any new block must exclude the core directory, and that constraint is currently recorded only in a code comment.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-design-system-foundation.md`
  summary: No PWA chrome, no `prefers-reduced-motion` guard, and no `color-mix()` fallback.
  evidence: CLAUDE.md calls the product PWA-first, yet there is no manifest, no `themeColor` viewport export and no `<meta name="theme-color">`, so browser chrome stays light while the app is dark. `tw-animate-css` was newly imported and dialog animations use it with no reduced-motion guard. Every `--ui-*` surface value and `--color-divider` — applied globally through `border-border` — is `color-mix()`, which is invalid at computed-value time on engines without support; the production minifier does emit solid fallbacks, but the resulting colours are flattened. Relevant for Indonesian field staff on older Android WebViews.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-design-system-foundation.md`
  summary: The design system is documented nowhere a contributor is pointed at.
  evidence: CLAUDE.md's "Where things are" map lists only `docs/01`–`docs/07`, none of which were touched. The `@theme inline` trap, the `accent`→`brand` rename, the forbidden ramp steps and the arbitrary-value rule live only in CSS comments and in `_bmad-output/specs/.../design-system.md`, which CLAUDE.md never mentions. The same gap applies to the `AD-*` identifiers cited throughout the code.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-the-application-shell.md`
  summary: The application shell itself — sidebar, header, five responsive bands, off-canvas drawer, theme toggle and the app-level wrapper layer — was split out of the DOM-test-harness spec and remains Story 1.3 in sprint tracking.
  evidence: The combined spec measured ~2,700 tokens against a 1,600 target, and the harness is independently shippable: it can be reviewed, tested and merged on its own, and the shell's acceptance criteria (44px touch targets, focus trap, tooltip on focus) cannot be verified until it exists. Its draft spec is on disk and narrowed to the shell alone.

- source_spec: `_bmad-output/implementation-artifacts/spec-dom-test-harness.md`
  summary: `REQUIRED_SUITES` proves a test file exists but never that it contains tests, so a suite emptied in place stays green.
  evidence: The guard is an `existsSync` loop. Replacing any required suite with an empty file satisfies it completely. This is the Story 1.1 pattern inherited unchanged, not a regression introduced here, but it caps what every mutation guard built on top of it can promise.

- source_spec: `_bmad-output/implementation-artifacts/spec-dom-test-harness.md`
  summary: Vitest 4 treats an unmatched `--project` filter as a silent no-op — `vitest run --project doesnotexist` exits 0 with a green report.
  evidence: Verified by a reviewer against the live tree. It means the `--project chromium` flag in the `test` script self-checks nothing; the entire guarantee that the browser project runs rests on `tests/harness-registration.test.ts` executing inside `unit`. That works today, but the margin is thinner than the config comments imply, and the same hazard applies to every future project filter in this repo.

- source_spec: `_bmad-output/implementation-artifacts/spec-dom-test-harness.md`
  summary: `optimizeDeps.include` is a hand-maintained list whose drift is only observable on a cold Vite cache.
  evidence: The list exists because a cold cache made Vite re-optimise mid-test and fail with `Failed to fetch dynamically imported module`. Nothing detects a newly tested component pulling in an unlisted dependency, and a warm local run cannot surface it — the failure returns as an intermittent CI import error. The shell story adds several components and is the first place this will bite.

- source_spec: `_bmad-output/implementation-artifacts/spec-dom-test-harness.md`
  summary: `@testing-library/user-event` is installed but imported nowhere.
  evidence: The spec's task named six dependencies; the focus-trap test correctly uses `userEvent` from `vitest/browser` instead, because that one drives real Playwright key input rather than dispatching synthetic events. The package is dead weight until the shell tests need it, which they may not.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-the-application-shell.md`
  summary: `next/link` in the browser test project resolves to the Pages Router implementation, so the module under test is not the one the build ships.
  evidence: Vite does not apply Next's app-dir alias, which is why the suite needs `tests/browser/next-env-shim.ts` and a capture-phase `preventDefault` to stop the iframe navigating. Component behaviour is asserted correctly, but every navigation assertion runs against a different module than production. The shim's must-be-first-import ordering is also enforced only by a comment rather than a `setupFiles` entry.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-the-application-shell.md`
  summary: The `--color-popover` exemption in the undeclared-variable sweep is keyed on the variable name, not on whether the component that draws it is rendered.
  evidence: Its stated justification is that `DrawerContent` is never rendered — true today, and nothing asserts it stays true. The name is an `@theme inline` key, so it emits no custom property; the moment a later story renders `DrawerContent`, its bleed layer resolves to nothing while the sweep stays green. The sweep is the repo's stated last line of defence against the stylesheet dropping a name a component still reads.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-the-application-shell.md`
  summary: `prefers-reduced-motion` is honoured only by the drawer; the nav item's `transition-colors` and the tooltip's enter animation are unguarded.
  evidence: The story's matrix names only the drawer slide, so this is outside its scope rather than a miss — but the repo now has one guarded animation and two unguarded ones, and the reduced-motion test measures only the drawer panel and overlay, so the gap is invisible to it.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-the-application-shell.md`
  summary: The drawer offers no close button — only backdrop, `Esc`, and following a link.
  evidence: Those three exits are exactly what `screen-dashboard-interaction.md` specifies, so the implementation is faithful to the contract. But the contract itself omits the affordance a touch user reaches for first, and this drawer is the mobile navigation for field staff on personal phones. A design decision to revisit, not an implementation defect.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-the-application-shell.md`
  summary: `company.membershipCount` is carried by the fixture and read by nothing.
  evidence: `CompanySwitcher` hardcodes the one-membership form rather than branching on it, so a `membershipCount: 2` fixture would silently render the wrong thing. The multi-company panel belongs to Story 1.6; the branch and its test belong there with it.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-the-application-shell.md`
  summary: The header date never rolls over at midnight on a long-lived tab.
  evidence: `useSyncExternalStore` is subscribed to a never-firing store, deliberately, so the value is a mount-time snapshot. In a product whose day boundary drives attendance and payroll periods, a tab left open overnight shows yesterday's date with no correction.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-the-application-shell.md`
  summary: The product's screen inventory now exists only in `components/shell/navigation.ts`.
  evidence: Ten destinations and a four-group information architecture were introduced without a line in `docs/05-modules.md` or anywhere else CLAUDE.md points a contributor. Two icon libraries also now ship — `lucide-react` survives, reachable from `components/ui/dialog.tsx` alone — with nothing recording whether it is being retired.

- source_spec: `_bmad-output/implementation-artifacts/spec-dom-test-harness.md`
  summary: The Playwright cache key is invalidated by any dependency change, not only a Playwright upgrade, and has no `restore-keys`.
  evidence: Observed on CI run 33045453865 — `Cache not found` because Story 1.3 added `@phosphor-icons/react`, changing the `package-lock.json` hash and forcing a fresh 114.7 MiB browser download. A reviewer raised this during the harness round and I rejected it on the grounds that the lockfile contains Playwright's version, which is true but incomplete: the key is correct yet far more sensitive than it needs to be. Keying on the resolved Playwright version, or adding `restore-keys`, would keep the browser across unrelated dependency bumps. Cost is ~20s per lockfile change, so this is cheap to leave and cheap to fix.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-tenant-isolation-harness.md`
  summary: The isolation gate cannot see a Supabase-specific permission failure, because bare Postgres is the more permissive substrate. A migration green in CI can still fail to apply to the real project.
  evidence: The gate runs `supabase migration up` against a stock `postgres:17` container — chosen deliberately, and measured: 31s cold against 541s for `supabase start`, 161MB against ~8.5GB across twelve containers, a full drop→migrate→sweep cycle in 2.22s against 15–26s. It proves every acceptance criterion, because all five are catalog or SQL facts. What it cannot prove is *applicability*: the container grants the migration role more than Supabase does. This is not hypothetical — `create function auth.tenant_id()` fails on a real project with `permission denied for schema auth (SQLSTATE 42501)` (migrations run as `postgres`; `auth` is owned by `supabase_admin`), and that exact defect sat in two architecture documents for a week. The mitigation shipped with this story is portability discipline in the migration itself: nothing is created outside `public`, and `anon`/`authenticated` creation is guarded with `do $$ … $$` because `create role` has no `if not exists` and both already exist on Supabase. The residual risk stands: the first `supabase db push` against a real project is still the first time these files meet the stricter substrate. Closing it needs either a scheduled job that applies migrations to a throwaway Supabase branch, or a lint pass over migration SQL for the specific things Supabase denies.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-tenant-isolation-harness.md`
  summary: The epic asks the isolation suite to assert **in-tenant** isolation per role tier; this story cannot, because no role tier exists yet.
  evidence: `epic-1-context.md` says "From this epic it also asserts in-tenant isolation per role tier" — `hr_staff` must not see salary, `staff` only its own row, `supervisor` only their reports. All of that needs `memberships` and `employees`, which Story 1.6 and Story 1.8 create; this story's Never list scopes it to `organizations` and `companies`. The substrate is ready for it: role-tier isolation inside one tenant was proved reachable on bare Postgres during this story's measurement pass, and `tests/isolation/support/substrate.ts` already switches roles per transaction. Story 1.6 must extend `Principal` with the claim-carried `role` and add the tier cases; nothing today would notice if it did not.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-tenant-isolation-harness.md`
  summary: `supabase_migrations` is excluded from catalog discovery outside the three-entry exemption allowlist.
  evidence: `RUNNER_SCHEMAS` in `tests/isolation/support/catalog.ts` drops the schema `supabase migration up` writes its own version rows into. It is not an application schema and holds no tenant data, so it is not an exemption from the tenant rule so much as a statement of where "our schema" ends — but it is a second place, next to `EXEMPTIONS`, where a name can be added to make the sweep look away. The exemption list is pinned to exactly three entries by a test; `RUNNER_SCHEMAS` is not pinned to one. Worth a decision at the next isolation-touching story rather than a silent precedent.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-tenant-isolation-harness.md`
  summary: ~~`CLAUDE.md` rule 2 says `tenant_id` must lead **every** index~~ — **RESOLVED 2026-08-27 by the project owner.** Rule 2 now states the enforceable form, and the two downstream copies in `epics.md` NFR-1 and the spine's inherited-invariants table were corrected with it.
  evidence: The strict reading is unsatisfiable as written: every table's `id uuid primary key` is a unique index whose leading column is `id`, so no table in the schema could ever comply, and Story 1.6's `memberships unique (user_id, company_id)` would violate it too. The enforced rule — every relation has at least one valid, non-partial index whose first column is the column its policy is keyed on — is the defensible one, and a reviewer agreed the document is what should move. It was left alone deliberately: `CLAUDE.md` is the project charter, and rewriting a non-negotiable rule on a reviewer's say-so is exactly the kind of change that should carry a human signature. Suggested replacement text: "RLS on every table, and every table must carry an index whose leading column is `tenant_id` — the access path every policy takes."

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-tenant-isolation-harness.md`
  summary: ~~The first migration raises the Postgres floor to **16**, and nothing enforces that the Supabase project is provisioned there~~ — **VERIFIED 2026-08-27**: the project reports `PostgreSQL 17.6`, matching the `postgres:17` gate exactly. Still unenforced for any *future* project.
  evidence: `public.tenant_id()` and `public.auth_user_id()` use the `IS JSON` predicate (PG16+) to fail closed on a malformed claim. The alternative — a plpgsql `EXCEPTION` block — was rejected because an exception block opens a subtransaction, subtransactions are forbidden inside parallel workers, and the functions must be `parallel safe` or a single call disables parallel query for the whole statement. So "fails closed on a malformed claim" and "parallel safe" are only simultaneously satisfiable on PG16+. The CI container is `postgres:17`, so the gate can never catch a project provisioned on 15; that check has to happen at provisioning time.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-tenant-isolation-harness.md`
  summary: `companies` is deliberately readable **above** the active tenant claim, by the owner of its organization. That is a widening of the tenant boundary and it needs revisiting in Story 1.6.
  evidence: A fresh signup holds a `sub` and no `tenant_id`, so `insert into companies ... returning id` failed under a policy keyed on `id = tenant_id()` — the insert is permitted, the *readback* is not, and PostgREST sends `Prefer: return=representation` by default. Reproduced both ways. `companies_visible_to_org_owner` fixes it by keying SELECT on organization ownership, which is also what the company switcher needs (it must render the legal name and branch count of companies the user is *not* currently in). The consequence is that one principal owning two organizations sees both their companies while holding one tenant claim. That is correct for the boundary row and wrong for anything below it, and today's fixture cannot tell the difference because A and B own disjoint organizations. Story 1.6 replaces "organizations you own" with "companies you hold an active membership in", and should add a fixture where one principal spans two tenants so the distinction is actually asserted.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-tenant-isolation-harness.md`
  summary: The materialized-view rule is privilege-based and has never met a real aggregate.
  evidence: Postgres has no `alter materialized view ... enable row level security`, and a matview is populated as its owner at REFRESH time, so its only protection is that no request role can select it. The sweep enforces exactly that, and a negative control proves the rule fires. But the architecture calls for "aggregates from materialized views only", so Epic 3 will need a wrapping pattern — a `security_invoker` view over the matview, or a function — and none exists yet. The first team to add a matview will hit a red gate with no worked example to copy.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-tenant-isolation-harness.md`
  summary: The `stat_*` exemption is the one allowlist entry that lifts the purity assertion itself, not just a structural rule.
  evidence: `tenant_fixture` was added to `WaivableRule` so global statutory tables are not required to seed two tenants — they have no tenant dimension, so the assertion would be meaningless. It is defensible for `stat_*` and it is also the most dangerous waiver in the file: any future exemption claiming it removes the behavioural check entirely. `EXEMPTIONS` is pinned to exactly three by a test, which is what currently contains it. Nothing yet asserts that `stat_*` policies are read-only, which is the property that actually protects them; that belongs with Epic 5, when the tables exist.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-tenant-isolation-harness.md`
  summary: ~~`CLAUDE.md` rule 1 says `tenant_id` is on every table with "No exceptions"~~ — **RESOLVED 2026-08-27 by the project owner**, alongside rule 2.
  evidence: Three exemptions were already specified and enforced when the rule still read "No exceptions": `stat_*`, the pg-boss schema, and the two tables at or above the tenant boundary. Rule 1 now names all three with their reasons and points at the pinned list in `tests/isolation/support/catalog.ts`; the definition-of-done checklist was tightened to match, since its first item was the same rule in shorter form and omitted both `force` and the leading index.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-tenant-isolation-harness.md`
  summary: `ltree` is installed in `public`; Supabase's own linter flags it, and moving it to the `extensions` schema is blocked on a `search_path` question only Story 1.7 can answer.
  evidence: Applying the migration to the real project (2026-08-27) produced exactly one advisory — `extension_in_public` WARN — which the bare-Postgres gate cannot produce, because in plain Postgres `create extension ltree` in `public` is simply correct. This is the first confirmed instance of the residual gap the harness spec recorded: the gate is the more permissive substrate. The fix is not one line. `postgres` carries `search_path = "$user", public, extensions`, but `authenticated` and `anon` have **no** `search_path` setting at all and fall back to `"$user", public` — so moving the extension would leave ltree operators unresolvable for exactly the roles that run requests, and the migration's own comment places an ltree operator on "every approval-routing request" in Story 1.7. Deferred there deliberately, on the owner's decision: Story 1.7 is the first place a real ltree query exists to prove a `search_path` change works, rather than theorising one now. Until then the WARN stands and is intentional.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-tenant-isolation-harness.md`
  summary: Nothing in CI applies migrations to the real Supabase project, so the portability gap closes only when a human remembers to.
  evidence: The isolation job runs against `postgres:17` in a service container. Applying the same migration to the live project on 2026-08-27 was a manual act, and it immediately surfaced a finding the gate cannot (`extension_in_public`). Everything else matched: RLS enabled and forced on both tables, both claim functions `parallel safe` with `search_path=""` and `prosecdef=false`, and all six malformed-claim states failing closed. A `supabase db push --dry-run` against a preview branch, or `get_advisors` in a scheduled job, would make that check routine instead of remembered.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-sign-up-and-create-a-company.md`
  summary: `zod` was added to `package.json` although the spec's task line said "no other new dependency". It adds nothing to the install graph.
  evidence: The frozen Boundaries block requires "Zod validation at every route-handler boundary", and no route handler can import a package the manifest does not declare — an undeclared import works today only because `shadcn` and `eslint-config-next` hoist `zod@4.4.3` into the tree. Declaring it pins the version already resolved: `npm install` reported ten added packages, all of them Supabase's, and `npm ls zod` shows one deduped copy. The alternative — importing a transitive dependency — breaks the day `shadcn` moves to `devDependencies` or drops zod, and a production build would fail with no warning. Flagged rather than assumed: the two lines of the spec disagree and this reading favours the frozen one.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-sign-up-and-create-a-company.md`
  summary: ~~There is no sign-in route.~~ — **RESOLVED 2026-08-27.** `/signin` and `POST /api/auth/signin` exist. Sign-*out* still does not, which is the remaining half.
  evidence: The lockout was real: `signUp` for a known address returns a response deliberately indistinguishable from a new signup and carrying **no session**, so signup could never be the way back in — that is asserted directly now, as the reason the route has to exist. The new route validates with Zod at the boundary, answers one message for every failure so it is not an account-existence oracle, and decides the landing path server-side: a half-registered user (signed up, company insert failed, browser closed) comes back to `/company/new` rather than to a dashboard whose shell has no company. It deliberately does **not** check for an existing session before the body — that would refuse a caller holding auth cookies whose refresh token has expired, which is the exact lockout it exists to end; the "already signed in" case is handled on the page, which redirects. What is still missing is sign-out: there is no way to leave an account, so a shared or handed-over browser stays signed in as the first person who used it, and a developer verifying sign-in has to clear cookies by hand. Small, and deliberately not added here because it was outside the requested patch.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-sign-up-and-create-a-company.md`
  summary: ~~No middleware refreshes the access token.~~ — **RESOLVED 2026-08-27.** `proxy.ts` refreshes and persists; what remains is that it costs one extra `/auth/v1/user` round trip per navigation.
  evidence: Urgent because `jwt_exp` was lowered from 3600 to 900 on the owner's instruction the same day, so a session now expires four times sooner than the code around it assumed — a user typing into the company registration form crosses the boundary in fifteen minutes. `tests/session-refresh.test.ts` proves the refresh rather than the file: it drives the real `@supabase/ssr` and `@supabase/auth-js` against a real expired-session cookie with only `fetch` stubbed, and asserts the token endpoint was called, the response carries the new access token, and the *request* carries it too so the render in the same pass is not the one signed-out render. Three mutations were checked and each turns it red: dropping the response write, dropping the request forward, and swapping `getUser()` for `getSession()`. That last one corrected a wrong comment — `getSession()` refreshes an expired token too, so the reason for `getUser()` is revalidation against the auth server, not the refresh, and the test now pins that instead. Residual cost: `getUser()` in middleware plus `currentUser()` in the render is two `/auth/v1/user` calls per navigation. The cheap fix (`getSession()` in middleware) relies on a refresh happening as a side effect of a call whose contract does not promise one, so it was not taken.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-sign-up-and-create-a-company.md`
  summary: ~~`Asia/Pontianak` is West Kalimantan, is WIB, and cannot be chosen at registration.~~ — **RESOLVED 2026-08-27.** The set is now four identifiers, and the spec's "three Indonesian zones" was corrected: three *zones*, four IANA identifiers.
  evidence: A West Kalimantan company could not register at all — `register_company` raised `23514` on the check constraint and there was no other value it could honestly have chosen. `components/shell/timezone.ts` has carried the warning since Story 1.3 ("the fourth is the one that gets forgotten") and the spec forgot it anyway. Fixed in the unapplied migration `20260827120000_…` rather than by stacking a third file, since it has never reached the live project; `20260827000000_…` was not touched. Proved red-before/green-after: `accepts Asia/Pontianak` failed with `23514` before the change and passes after. A new assertion ties `COMPANY_TIME_ZONE_IDS` to `INDONESIAN_TIME_ZONES` in the shell, because those two lists were written a story apart and disagreeing is what caused this — a company stored with an identifier the header cannot label renders the raw identifier where every other tenant sees a zone name.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-sign-up-and-create-a-company.md`
  summary: `supabase/migrations/20260827120000_signup_register_company.sql` has NOT been applied to the live project, and the live migration ledger already disagrees with the repository's file names.
  evidence: |
    Two separate facts, and the second one blocks the first.

    **The ledger disagrees with the repository.** `supabase_migrations.schema_migrations` on the live project holds exactly one row — `version 20260827093808`, `name tenant_isolation_foundation`, one statement — while the repository file is `supabase/migrations/20260827000000_tenant_isolation_foundation.sql`. The CLI keys on the *version*, not the name, so it sees a local migration that has never been applied (`20260827000000`) and a remote migration that does not exist locally (`20260827093808`). Cause: Story 1.4's migration was applied through the MCP `apply_migration` tool, which stamps its own timestamp rather than using the file's. The schema itself is correct and already verified — RLS enabled and forced on both tables, both claim functions `parallel safe` with `search_path=""` and `prosecdef=false`.

    **What a human must run.** `supabase migration repair` writes only to `supabase_migrations.schema_migrations`; it executes **no DDL**, so neither step below changes a single object in the database. Verify each step's output before running the next.

        # 0. Confirm the disagreement before changing anything.
        supabase link --project-ref vkhurdpfbrqdekwmgzap
        supabase migration list --linked
        #    Expect: 20260827000000 under Local with no Remote,
        #            20260827093808 under Remote with no Local,
        #            20260827120000 under Local with no Remote.

        # 1. Drop the phantom version. Deletes one ledger row. No DDL.
        supabase migration repair --linked --status reverted 20260827093808

        # 2. Record the file whose effects are already in the database.
        #    Inserts one ledger row. Does NOT run the file.
        supabase migration repair --linked --status applied 20260827000000

        # 3. Confirm Local and Remote now agree on the first two.
        supabase migration list --linked

        # 4. Apply the genuinely unapplied migrations, oldest first.
        supabase db push
        #    Expect exactly two files applied, in this order:
        #      20260827120000_signup_register_company.sql
        #      20260827140000_tighten_write_surface.sql
        #    The second narrows grants made by the first migration in the repo
        #    and adds length constraints; it depends on nothing the first adds,
        #    but the order is the file order and the CLI applies it that way.

        # 5. Confirm the paid tier is no longer self-service.
        #    As the `authenticated` role this must now fail with 42501.
        #      update public.organizations set plan = 'payroll';

    Both unapplied files are re-runnable — every `add constraint` is guarded on `pg_constraint`, and `create or replace`, `grant` and `revoke` are idempotent by nature — so a push that repeats one raises nothing. That is deliberate insurance for exactly this runbook: a bare `alter table … add constraint` raises 42710 the second time and takes the whole push down with it.

    Step 2 is safe **only because** the live schema already matches `20260827000000_…` exactly; that was verified on 2026-08-27 and must be re-verified if anything has touched the project since. If it no longer matches, repairing the ledger would hide a real drift, and the right move is to inspect the schema first rather than to repair.

    **The new migration is unapplied and verified applicable.** Applying it here was blocked by the environment's permission classifier, so it was checked by inspection: the live project reports `postgres` as owner of both `organizations` and `companies` and as holding CREATE on `public`, which is everything the file needs — it touches no schema but `public` and creates one function and two check constraints. Both tables are empty (`orgs 0, companies 0, users 0`), so neither constraint can fail on existing data. This is the third instance of the gap already recorded: nothing in CI applies migrations to the real project, so the check happens only when someone remembers.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-sign-up-and-create-a-company.md`
  summary: The confirmation link has never been exercised end to end. `exchangeCodeForSession` has run against no real email.
  evidence: The project can send two confirmation emails an hour in total (`rate_limit_email_sent: 2`, `smtp_host: null`), so a live signup spends a scarce, shared resource — and the "check your email" screen now says so. Everything around the exchange is proved: `/signup`, `/company/new` and `/` behave correctly against the live project, the callback's three failure paths each redirect to a stated error, and the open-redirect guard rejects `next=//evil.example`. What is unproved is the one hop that needs a real link: whether Supabase's default template returns `?code=` (PKCE) rather than a `#access_token` fragment for this project's settings. If it returns a fragment, the callback answers `link_missing` and the flow stops there. First real signup is the test.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-sign-up-and-create-a-company.md`
  summary: Everything under `app/(app)` is now session-gated, which turned eleven previously-static placeholder routes into dynamic ones.
  evidence: `app/(app)/layout.tsx` calls `requireUser()` and reads the company, both of which touch `cookies()`, so `next build` now reports every route under the group as `ƒ (Dynamic)`. That is correct — none of those screens means anything without a tenant — but it is a change the story did not name, it removes prerendering from screens that will later want it, and it makes the shell's first paint wait on two Supabase round trips. Worth revisiting when the dashboard has real content to cache.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-sign-up-and-create-a-company.md`
  summary: `tests/isolation/signup-rpc.test.ts` is the first isolation suite that **commits** rows, and it shares one database with suites running in parallel workers.
  evidence: Proving atomicity requires a write that survives the statement that failed, so the suite cannot roll back the way `tenant-purity.test.ts` does. It confines itself to five user ids nothing else uses and deletes their rows in `afterEach`, and the existing suites are keyed on their own principals, so the two cannot see each other today. The fragility is structural rather than present: a future suite that counts rows globally, or a crash between the write and the cleanup, would leave the substrate dirty for the next file. `globalSetup` drops and re-migrates on every run, so the blast radius is one run.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-sign-up-and-create-a-company.md`
  summary: Reading `process.env` before a request-time API turns a dynamic route into a prerender failure, and `npm run build` is the only gate that sees it — because it is the only one that runs without a `.env.local`.
  evidence: Hit during this story. `lib/supabase/server.ts` originally awaited `cookies()` and then read the key; the reads were hoisted above it to make the elevated-key guard easier to assert, and the build immediately failed with `NEXT_PUBLIC_SUPABASE_URL is not set` while prerendering `/approvals`. The mechanism is not obvious: `cookies()` throws a signal during a prerender that Next catches to mark the route dynamic, so anything evaluated *before* it runs in a context that was never meant to execute at build time. Lint, typecheck, the unit project and the isolation project all stayed green, and every local build passed because `.env.local` exists on a developer's machine. The ordering is now pinned by a test that observes the request being consulted with the environment missing, and the code comments say why in both files. The general rule for every module added later: touch the request first, read configuration second. Worth stating somewhere a contributor is pointed at — `docs/07` would be the place — rather than only in two file headers.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-sign-up-and-create-a-company.md`
  summary: Sign-in has no rate limiting of its own, and GoTrue's per-IP limit does not help because every request reaches it from one address.
  evidence: No captcha, no lockout, no counter — an attacker may try passwords as fast as the network allows. The instinct is that Supabase already limits this, and it does, per IP: `/api/auth/signin` calls `signInWithPassword` **server-side**, so every attempt in the product arrives at GoTrue from the deployment's egress address. That turns a per-attacker bucket into one bucket shared by every customer, which is worse than no limit in both directions — no defence at all against credential stuffing spread across many source addresses, and a self-inflicted outage for legitimate users the moment somebody trips it. Fixing it needs a counter keyed on something the server can see (the submitted address, and the true client IP from the forwarded chain now that `trustedOrigin` establishes which proxy is ours), plus a decision about lockout versus delay versus captcha. That is a design question with a real usability cost, not a patch, and it wants the same treatment the password-reset flow will need.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-sign-up-and-create-a-company.md`
  summary: The sign-in timing side-channel is unmeasured, and the reviewer who found it declined to measure it rather than guess.
  evidence: `/api/auth/signin` returns one message for every failure, which closes the *content* oracle. It does not close a timing one: an unknown address may return before a hash is computed while a known address with a wrong password returns after, and that difference is enumeration by stopwatch. Whether it is observable here is genuinely unknown — the work happens inside GoTrue, across a network hop, and the network noise may well exceed the signal. It was not tested because the only substrate that could answer is the live project, where every attempt writes an audit row, and the reviewer said so instead of asserting a conclusion. Measuring it belongs with the rate-limiting work above, on a throwaway project, with enough samples to separate the distributions rather than one comparison.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-sign-up-and-create-a-company.md`
  summary: Password reset and sign-out do not exist — not in the code, not in the spec, and until now not here either.
  evidence: Two gaps of different shapes. **Sign-out** is small and its absence is visible: nothing clears the session, so a shared or handed-over browser stays signed in as whoever used it first, and a developer verifying sign-in has to clear cookies by hand. **Password reset** is not small: it needs a mail, and Aira can send two an hour across the entire project, so the flow that recovers an account competes for budget with the flow that creates one — and a user who cannot sign in and cannot reset is a user with no route back at all. That makes it the second thing, after the confirmation link itself, that a transactional mail provider gates. Both were absent from the story's task list and its I/O matrix, so neither is a regression; recording them is the point, because "absent from the spec" was the reason nobody had written them down.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-sign-up-and-create-a-company.md`
  summary: `NEXT_PUBLIC_SITE_URL` is new configuration. It is not a secret, but it is the single value that decides where a confirmation link points.
  evidence: The story's Ask First line covers "any new secret in `.env.local`", and this is not one — it is public by definition and carries the `NEXT_PUBLIC_` prefix. It is flagged anyway because of what depends on it: `trustedOrigin()` refuses to honour `x-forwarded-host` unless it matches, so an unset or wrong value in production is a deployment whose confirmation links point at the wrong host, and a right value is the only thing standing between a forged header and a PKCE code mailed to an attacker. It falls back to Vercel's own deployment host and then to `http://localhost:3000` outside production, and throws in production rather than trusting the request. `.env.example` documents it; `.env.local` was deliberately not edited.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-sign-up-and-create-a-company.md`
  summary: A request path can still create unlimited *empty* organizations, one per POST. Only the plan they carry is now fixed.
  evidence: Surfaced while closing the self-service upgrade. `20260827140000_tighten_write_surface.sql` removes `plan` from what `authenticated` may insert or update, so every organization a request creates is on `free` — the billing escalation is gone. What remains is the row count: `insert into organizations (name, owner_user_id)` is granted and the policy permits it, so a caller can create as many as they like. None of them is billable (billing follows employees under a company) and `register_company` never makes a second, so this is table growth rather than a hole. Constraining it means deciding whether one user may own several organizations at all — group HR and outsourcing vendors are the case the data model was built for — which is a product question, and CLAUDE.md puts "whether something belongs at organization or company level" on the ask-first list.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-sign-up-and-create-a-company.md`
  summary: The test-task property was complete and the suite was blind, and the same failure mode will recur on any story whose property is written from the inside out.
  evidence: The story named atomicity, retry, Zod, the credential, the field rules and session gating — all of which are about what happens *after* a request is accepted — and the suite satisfied every one of them while an open redirect, an unverified mail host and an absent CSRF defence sat on the front door. None of those needed a malformed body, an invalid schema or a missing session, which is precisely why nothing noticed. The amended property ("no request may cause a redirect off-origin, a mail to an unverified host, a state change without a same-origin intent, or a write the database itself would not permit") is the durable half of the fix; the generalisable lesson is that a property enumerating the code's own concerns cannot describe the surface an attacker sees, and that any story adding a request path needs the second kind of property written for it. Worth a line in `docs/07` beside the isolation gate rather than only here.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-sign-up-and-create-a-company.md`
  summary: Next 16 deprecated the `middleware` file convention; the session refresh ships as `proxy.ts`, and `AGENTS.md` is the only reason that was noticed.
  evidence: The refresh pass was first written as `middleware.ts`, which is the shape every Supabase and Next tutorial still shows. `next build` printed `The "middleware" file convention is deprecated. Please use "proxy" instead.` — and it printed it to stdout on a successful build, so lint, typecheck and both test projects stayed green and only reading the build output caught it. `AGENTS.md` says in as many words that this repository's Next is not the one in training data and that deprecation notices are to be heeded; that instruction is what turned a warning nobody reads into a rename. Behaviour is unchanged — the file name and the exported function name are all that differ — and it was written by hand rather than through `npx @next/codemod middleware-to-proxy`, because the file is forty lines and the codemod's output would have needed the same review. Worth noting for the next story: a Next deprecation surfaces only in build output, and nothing in CI fails on one.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-membership-and-tenant-context.md`
  summary: **Nothing creates the founding membership.** The hook, the switcher and the whole claim path work; no user has a row for them to work on.
  evidence: `register_company()` created an organization and a company and stopped there, so a freshly registered owner held a `sub`, owned a company, and had no `memberships` row — which meant the hook injected no `tenant_id`, and every tenant policy in the schema still evaluated to "see nothing" for them. The story's own boundaries are what prevented closing it inside the story: `tenant_id` and `role` must not be writable from a request path (which rules out granting INSERT plus a founding-membership policy), and making `register_company` `security definer` would be the third such function, which the spec puts on Ask First.
  resolved: The owner chose the dedicated-function option and it landed in the same migration. `public.create_founding_membership(uuid)` is `security definer`, takes a company and no user — so "create a membership for somebody else" is not a call that can be written — derives the subject from the JWT, checks ownership against `organizations.owner_user_id` explicitly because `security definer` skips the policy that would otherwise make that check, and is idempotent so a retry resumes. `register_company` stays `security invoker` and calls it, unconditionally rather than inside the "company was just created" branch, which makes re-running registration the repair path for an account that predates memberships. `memberships` keeps zero write surface: `has_table_privilege('authenticated', ...)` for INSERT, UPDATE and DELETE is still false, and `tests/isolation/founding-membership.test.ts` pins that alongside a before/after on the hook's output — `{}` before registering, a real `tenant_id` and `role: admin` after. Both the ownership check and the call site were mutation-tested (4 and 7 failures respectively). Epic 2's token-based invitation still needs its own decision for every membership after the first: this function is the FOUNDING one only, and its ownership check is exactly what makes it unusable for invitations.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-membership-and-tenant-context.md`
  summary: The hook exists in the migration and is **not enabled on the live project**, so no token carries a claim yet even where memberships exist.
  evidence: On the spec's Ask First line, because enabling it is a Management API write and enabling it before the function exists breaks every login. The order is: apply `20260827160000_memberships_and_tenant_context.sql`, verify `select public.custom_access_token_hook('{}'::jsonb)` answers, then set `hook_custom_access_token_enabled = true` and `hook_custom_access_token_uri = 'pg-functions://postgres/public/custom_access_token_hook'`. Both settings are `false`/`null` today. The reverse order is a total outage: GoTrue returns before `SignJWT` on any hook failure, with a hard 2s timeout and no retries, on sign-in **and** on `token_refresh` — so a broken hook does not merely block new logins, it evicts every signed-in user within the 15-minute TTL.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-membership-and-tenant-context.md`
  summary: The header now resolves membership-first and falls back to `getOwnedCompany()`. The fallback is dated, not permanent, and the date is stated below.
  evidence: Neither "repoint and backfill" nor "leave it" was right on its own. Repointing outright blanks the header for every account on the live project, since those accounts have no membership row and a backfill cannot be written from a request path — nothing there may write `tenant_id` or `role`. Leaving it breaks the opposite case: an invited member holds a real `tenant_id`, owns nothing, and would see "No company yet", and Epic 2's invitation flow is the story that creates them. `currentActiveCompany()` in `lib/auth/session.ts` therefore takes the entry from `switch_company()` whose id matches the tenant CLAIM, and falls back to `getOwnedCompany()` only when that finds nothing. It keys on the claim rather than on `companies[0]` deliberately: the list is ordered as the hook orders memberships, so its first entry is what the NEXT token will carry, and using it would put one company's name over another company's data for as long as a token outlives a switch — asserted with the list deliberately ordered against the claim. `switch_company()` gained `organizations.plan` so the membership path can render the plan line at all, because `organizations_owner` makes that row readable to the owner only. The returned `source` field is `"membership"` or `"ownership"`, so "does any account still resolve by ownership" is a question the data can be asked rather than assumed. **The fallback may be deleted once no account resolves by ownership.** Getting there needs either a repair pass — re-run `register_company` once per pre-membership account, which is now idempotent and creates the missing row — or simply waiting for each owner to next submit registration. `app/(app)/company/new/page.tsx` keeps calling `getOwnedCompany()` directly and correctly: it asks about ownership, not membership. `PRINCIPAL_MULTI` now exists in the fixture registry and belongs to both tenants, so the ownership-versus-membership distinction the Story 1.5 fixture could not express is asserted for `memberships`; `companies` still keys its readback on ownership.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-membership-and-tenant-context.md`
  summary: The isolation project no longer runs its files in parallel, and the race it was hiding predates this story.
  evidence: `tenant-purity.test.ts` counts rows as the admin and then counts what a tenant can see, asserting the second is smaller. Several suites commit rows to the same database — `write-surface` registers a company, `signup-rpc` writes and deletes five users, and now `membership-switching` seeds membership rows in a `beforeEach` — so an insert landing between those two counts makes a true property read as false. Reproduced as exactly that failure on `memberships`, intermittently, and the same shape was latent on `companies` from Story 1.5. Fixed with `fileParallelism: false` on the isolation project, which costs nothing at two seconds a run. The deeper fix, if the suite ever grows enough to want parallelism back, is for the non-vacuity assertion to be scoped to the fixture's own rows rather than to `count(*)`.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-membership-and-tenant-context.md`
  summary: In-tenant isolation per role tier is still unasserted — the claim now exists to key it on, and no policy uses it.
  evidence: `epic-1-context.md` says this epic must assert that `hr_staff` cannot see salary, `staff` sees only its own row and `supervisor` sees only their reports. Story 1.6 delivers the input those rules need — `role` is in `app_metadata` and reachable from a policy without touching `memberships` — but there is no table yet whose policy could branch on it: `employees` and its salary columns arrive in Story 1.8. `Principal` in `tests/isolation/support/substrate.ts` still carries only `userId` and `tenantId`, so the claim a tier test would need is not even expressible in the harness. Story 1.8 must extend `Principal` with the claim-carried role and add the tier cases; the earlier deferred entry naming Story 1.6 for this was written before it was clear the tables would not exist yet.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-membership-and-tenant-context.md`
  summary: `currentClaims()` adds a `getClaims()` call to every render behind the shell, on top of the two Supabase round trips the layout already makes.
  evidence: The role has to come from the token — that is the whole point of `AD-25`, and querying `memberships` to render it would reintroduce the per-request lookup the claim exists to remove. But `getClaims()` is a third call: with asymmetric signing keys it verifies locally against a cached JWKS, and with a symmetric secret it sends a request much like `getUser()`. Which of those applies to the live project has not been measured. Memoised per request with `cache()`, so one render makes one call, and the layout issues its three reads with `Promise.all` so they overlap rather than queue. Worth measuring once the project's signing-key mode is known, alongside the existing entry about `updateSession` calling `getUser()` on every navigation.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-membership-and-tenant-context.md`
  summary: `tenant-purity.test.ts`'s update probe now accepts a privilege refusal as well as zero rows, which widens what "protected" means for every table added after this one.
  evidence: `memberships` grants `authenticated` SELECT and nothing else, so the generic `update <relation> set <key> = <key>` probe raises `42501` instead of reporting `UPDATE 0`. The alternative was to grant an UPDATE privilege purely so the probe could resolve to a policy decision — which is a privilege that exists for a test, and on this table specifically the grant is the thing being defended against. The generalisation is sound (a table nothing may write is protected, and `readOrDenied` already accepted either mechanism for reads) but it is genuinely weaker: a future table that grants UPDATE and forgets its policy is still caught, while one that is protected only by an accidentally-missing grant now reads as deliberate. `updateOrDenied` keeps the two outcomes distinguishable, and `membership-switching.test.ts` asserts `denied === true` specifically where the refusal is the requirement.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-membership-and-tenant-context.md`
  summary: The founding membership is created *after* the account's first token is issued, so the very first session of every account is claimless until something reissues it. The reissue closes the window; it does not remove the ordering.
  evidence: Confirmation issues a token at `/auth/callback`, and the membership does not exist until the user submits `/company/new` — so the hook correctly returns `{}` for that first token, and correctly returns a real `tenant_id` for every one after it. `/api/companies` now calls `refreshSession()` after `register_company()`, which closes the gap for the path a person actually walks. What remains is that the ordering is inherent rather than fixed: any route that ends up between confirmation and registration, any client that reads claims before submitting the form, and any future flow that issues a token before a membership exists will see the same empty claim set. It was invisible for a long time because three correct behaviours covered for it — `UserBlock` omits a null role instead of inventing one, `currentActiveCompany()` falls back to organization ownership so the company still renders, and nothing queries tenant data yet. From Story 1.8 the symptom becomes a new user seeing an empty employee list that heals itself in fifteen minutes, which is close to unreportable. The durable fix is to stop having a token that predates the tenant at all — either by creating the membership as part of confirmation, or by treating "signed in with no tenant claim and no company" as an explicit onboarding state the shell recognises rather than a state it renders around. Neither is a Story 1.6 decision.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-membership-and-tenant-context.md`
  summary: `tests/tenant-context-reissue.test.ts` enforces the reissue rule by reading source text, and no layer in the project can prove a token was actually reissued.
  evidence: Recorded because the test is easy to over-trust. The isolation suite talks to Postgres directly and issues no tokens at all — it sets `request.jwt.claims` with `set_config`, so nothing there could notice a missing reissue. The browser suite renders the shell from fixtures and never has a session. The boundary suites mock the Supabase client, so `refreshSession` is whatever the mock says. Between them they cover *which* routes must reissue (structural), and *when* they call it relative to the write and what they do when it fails (behavioural, against a mock). What is uncovered is that GoTrue mints a token carrying the new claim — that needs a real auth server, which means either `supabase start` (rejected on cost in Story 1.4: 541s cold, ~8.5GB) or a disposable live project. Worth revisiting if a second defect of this shape appears; one is not yet evidence of a class.
