import { PlaceholderScreen } from "@/components/shell/placeholder-screen";

export const metadata = { title: "Configuration" };

export default function Page() {
  return (
    <PlaceholderScreen
      title="Configuration"
      note="Company structure, salary components and payroll settings are built in Stories 1.7 and Epic 5."
    />
  );
}
