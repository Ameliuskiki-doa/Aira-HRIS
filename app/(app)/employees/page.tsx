import { PlaceholderScreen } from "@/components/shell/placeholder-screen";

export const metadata = { title: "Employees" };

export default function Page() {
  return (
    <PlaceholderScreen
      title="Employees"
      note="The employee list, dated assignments and spreadsheet import are built in Stories 1.8 and 1.9."
    />
  );
}
